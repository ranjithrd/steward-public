{
	"scripts": {
		"cli": "tauber",
		"start:app": "cd app && yarn dev",
		"start:server": "cd server && yarn dev",
		"start": "run-p start:app start:server"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5",
		"tauber": "^0.0.2"
	},
	"dependencies": {}
}
